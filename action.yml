name: 'Hello World'
description: 'Greet someone'
container: gitlab-registry.in2p3.fr/escape2020/wp3/zenodoci:v1.2
inputs:
  output_dir:  # id of input
    description: 'Output directory'
    required: true
    default: 'build'
outputs:
  random-number:
    description: "Random number"
    value: ${{ steps.random-number-generator.outputs.random-id }}
runs:
  using: "composite"
  steps:
    - run: |
        ${{ github.action_path }}/zenodo_prepare_upload.sh ${{ inputs.output_dir }}
        upload_new_deposit --token ${{ secrets.SANDBOX_ZENODO_TOKEN }} --sandbox True --input-dir ${{ inputs.output_dir }}
      shell: bash
    - id: random-number-generator
      run: echo "::set-output name=random-id::$(echo $RANDOM)"
      shell: bash
    - run: ${{ github.action_path }}/goodbye.sh
      shell: bash
